# gpustat4cluster 开发路线图（基于当前开发设计文档）

> 目标：在保证“低延迟、低 CPU 开销、运维简单可靠”的前提下，分阶段落地 server/client 双包、零拷贝传输、请求合并缓存与 CLI 增量渲染。

## 0. 总体里程碑（建议 6 个阶段）

- **M0：架构冻结与接口契约**（1 周）
- **M1：最小可用链路 MVP**（2 周）
- **M2：缓存/请求合并/并发模型增强**（2 周）
- **M3：CLI 渲染与过滤能力完善**（1.5 周）
- **M4：可运维性与发布工程化**（1.5 周）
- **M5：压测、稳定性与 GA 发布**（2 周）

总计约 **10 周**（可并行压缩至 6~8 周）。

---

## 1. 架构分层与目录规划（M0）

### 1.1 仓库建议结构（单仓多 crate）

- `crates/common/`：协议结构、配置结构、错误码、日志字段定义
- `crates/server/`：服务端守护进程
- `crates/client-backend/`：客户端后端守护进程
- `crates/client-cli/`：命令行前端
- `packaging/`：deb/rpm 构建脚本、systemd、postinst/preinst
- `scripts/`：压测、回归、CI 工具脚本
- `docs/`：协议文档、运维手册、性能基线

### 1.2 关键技术栈冻结

- Rust stable + tokio
- `nvml_wrapper`（GPU 数据采集）
- `rkyv` + `AlignedVec`（归档与零拷贝）
- `bytes::Bytes`（共享缓冲）
- KCP（连接层）
- UDP 多播（服务发现）
- `RwLock` + 条件通知（缓存/请求合并）
- 滚动日志库（按 `max_size`）

### 1.3 接口契约先行

优先冻结以下契约，避免后续返工：

1. 握手消息：hostname、gpu_num(u8)、payload_len(u16，固定长度)
2. 数据 payload：`INT64 timestamp_ms + ArchivedServerGPUS bytes`
3. 查询请求/响应类型与错误码（超时、断连、非法过滤器等）
4. 配置 `config.toml` schema 与默认值

---

## 2. M1：最小可用链路 MVP（端到端可跑）

### 2.1 服务端 MVP

- 从 NVML 拉取全量 GPU 状态
- rkyv 归档为 `AlignedVec`
- 监听端口在 `[30000, 40000]` 自动探测
- UDP 多播上线通告（IP/port/基础元数据）
- 单连接 KCP 会话建立与基础握手
- 周期发送固定 payload（先允许固定间隔刷新）

### 2.2 客户端后端 MVP

- 多播发现服务端（默认收集窗口 5 秒）
- 为每个发现节点建立 KCP 连接
- 处理握手并建立连接元数据
- 接收固定 payload 并写入连接缓存
- 提供本地 query API 给 CLI

### 2.3 CLI MVP

- 基础表格展示：hostname、GPU 利用率、显存、进程
- 初版参数兼容：`-n`、`-user`
- 定时刷新（先全量刷新，后续升级为局部刷新）
- 对齐并复刻 `gpustat` CLI 输入/输出习惯（参数行为、列布局、显示语义），并调研其 `pynvml` 调用路径以指导 Rust/NVML 字段映射

### 2.4 M1 验收标准

- 1 个客户端可稳定拉取 10 个服务端数据
- 连续运行 24h 无崩溃
- 断开/重连可自动恢复

---

## 3. M2：并发模型、缓存与请求合并（性能核心）

### 3.1 服务端缓存模型落地

- 全局 `RwLock<CacheEntry>`：最新归档 bytes + timestamp
- 惰性刷新：仅当缓存空或超过 `cache_ttl_ms`
- 多 reader 单 writer：reader 无锁快读、writer 短临界区更新

### 3.2 服务端请求合并

- 每连接维护请求队列 + lookup 循环
- 查询命中缓存直接返回
- 缓存过期时等待同一次 writer 刷新结果并广播唤醒
- 避免丢失唤醒：循环退出前二次检查队列

### 3.3 客户端连接私有缓存

- 每连接记录：connectionId、hostname、num、Bytes、recordTimestamp
- 命中查询时按 TTL 判断有效性
- 失效触发向对应服务端拉新
- 断连立即释放缓存和缓冲区

### 3.4 M2 验收标准（关键）

- NVML QPS 随客户端并发增加接近“亚线性增长”
- 缓存命中率在高频查询场景 > 90%（默认 TTL 下）
- 单节点 CPU 开销显著低于 JSON 方案

---

## 4. M3：CLI 渲染体验与过滤器完善

### 4.1 增量局部刷新

- 预分配终端渲染区域
- 按服务端区块刷新，timestamp 不变则跳过
- 避免全屏闪烁

### 4.2 `-n` 过滤器完整语法

- 支持 IP / 主机名 / 逗号 / 方括号范围 / 通配符
- 先解析 hostname->IP，再合并去重
- 维护“过滤项 -> 连接”映射，减少重复查询

### 4.3 `-user` 显示过滤

- 后端仍查询全量
- 前端仅做渲染过滤，确保数据完整

### 4.4 M3 验收标准

- 100+ 节点列表刷新可用，肉眼无明显闪烁
- 复杂 `-n` 表达式解析正确率 100%（基于测试集）

---

## 5. M4：运维与发布工程化

### 5.1 分包与安装

- 产出 `gpustat4cluster-server` / `gpustat4cluster-client`
- 安装时创建 `/etc/gpustat4cluster/config.toml`
- 自动创建 `gpustat4cluster` 系统用户
- systemd 服务启用与最小权限运行（非 root）

### 5.2 观测与日志

- 滚动日志：`max_size` 生效
- 启动首行统计：合并次数、命中率、平均 NVML 延迟
- 事件日志：连接建立/断开/异常
- 日志级别支持 Info/Debug 动态切换

### 5.3 配置校验与热更新策略

- 启动前 schema 校验（端口范围、TTL、多播地址）
- 明确哪些项支持热更新，哪些需重启

### 5.4 M4 验收标准

- 一键安装/卸载不残留脏状态
- systemd 重启后自动恢复
- 非 root 下完整可用

### 5.5 GitHub Release 与一键安装

- 使用 GitHub Actions Workflow 自动构建并发布 release 产物
- 产物矩阵至少覆盖：`server/client` × `linux-amd64/linux-arm64`
- 同时发布动态链接（gnu）与静态链接（musl）版本，系统目标为通用 Linux（anylinux 风格兼容）
- 提供一键安装脚本：
  - 交互式：可选安装 `server` / `client` / `both`
  - 非交互式：必须通过参数显式指定 `server` 或 `client`（可选 `both`）
  - 自动识别架构并下载对应二进制，写入标准路径并设置可执行权限
  - 自动安装并启用对应 systemd service，初始化 `/etc/gpustat4cluster/config.toml`
- gnu 动态链接安装流程需检测发行版并在缺失依赖时调用对应包管理器自动补齐（apt/yum/dnf/zypper 等）

### 5.6 启动健壮性与异常处理基线

- 程序启动必须覆盖并优雅处理关键异常：
  - NVML 不存在/驱动未安装/权限不足
  - GPU 设备为 0、驱动版本不兼容、NVML 初始化失败
  - 端口范围被占满、多播组加入失败、KCP 初始化失败
  - 配置文件缺失/损坏/字段非法
- 对每类异常提供：可读错误码、明确日志、可恢复建议（例如安装驱动、切换端口、降级运行）
- 明确“可降级运行”策略：例如 NVML 不可用时 server 以 `degraded` 状态对外报告并拒绝数据请求而非崩溃
- 在 systemd 下定义失败重试与退避策略，避免异常启动风暴

---

## 6. M5：测试、压测与 GA

### 6.1 测试矩阵

- 单元测试：协议序列化、过滤器解析、TTL 判断
- 集成测试：server/client 握手、断连重连、超时
- 回归测试：不同 GPU 数量与驱动版本

### 6.2 性能压测

- 场景：10/50/100/300 节点
- 指标：
  - E2E 刷新延迟（P50/P95/P99）
  - server CPU / 内存
  - 客户端聚合耗时
  - NVML 调用频次
  - 断连恢复时间

### 6.3 稳定性与故障演练

- 网络抖动、丢包、乱序（KCP 容错验证）
- 服务端进程重启、客户端 backend 重启
- 多播不可达时的降级行为（手动节点列表）

### 6.4 GA 出口标准

- 连续 7 天 soak test 稳定
- 关键指标达到预设 SLO
- 运维手册与排障手册齐备

---

## 7. 并行实施建议（缩短周期）

- **小队 A（协议/传输）**：KCP + rkyv + 握手契约
- **小队 B（采集/缓存）**：NVML、TTL、请求合并
- **小队 C（客户端/CLI）**：发现、聚合、增量渲染
- **小队 D（运维/发布）**：打包、systemd、日志、CI

并行关键点：M0 契约必须先冻结，A/B/C 才能同步推进。

---

## 8. 风险清单与缓解

1. **rkyv schema 演进兼容性风险**
   - 缓解：增加协议版本号，握手阶段协商版本
2. **KCP 每连接单线程在极端规模下线程数过多**
   - 缓解：预留“线程池 + 会话复用执行器”演进方案
3. **多播在部分网络受限**
   - 缓解：提供静态节点清单与单播发现后备机制
4. **NVML 调用抖动影响尾延迟**
   - 缓解：缓存合并 + 采集超时保护 + 指标监控
5. **终端渲染在不同 shell/TERM 表现差异**
   - 缓解：兼容层 + 自动降级为全量刷新模式

---

## 9. 第一迭代（未来两周）可执行任务清单

### Week 1

- 冻结协议与配置 schema
- 实现 server 采集 + rkyv 归档 + KCP 握手
- 实现 client 发现 + 建连 + payload 接收
- 打通最小 CLI 展示

### Week 2

- 接入服务端全局缓存与惰性刷新
- 接入客户端连接私有缓存与 TTL
- 实现请求合并 lookup 循环
- 输出第一版压测脚本与基线数据

**两周目标**：形成“可跑 + 可测 + 可观测”的 MVP baseline，为后续性能优化提供可比较基线。
