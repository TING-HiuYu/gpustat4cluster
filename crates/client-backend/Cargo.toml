[package]
name = "gpustat4cluster-client-backend"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "gpustat4cluster-client-backend"
path = "src/main.rs"

[dependencies]
common = { path = "../common" }
toml = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
