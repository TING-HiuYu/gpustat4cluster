[package]
name = "common"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "Common protocol/config types for clustat"

[dependencies]
crc32fast = "1"
rkyv = "0.8.16"
serde = { version = "1", features = ["derive"] }
thiserror = "1"
toml = "0.8"

[dev-dependencies]
serde_json = "1"
