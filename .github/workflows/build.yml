name: Build

on:
  workflow_dispatch:
    inputs:
      commit_target:
        description: "Commit, branch, or tag to build"
        required: true
        default: "main"
        type: string

permissions:
  contents: read

jobs:
  resolve-target:
    name: resolve build target
    runs-on: ubuntu-latest
    outputs:
      commit_sha: ${{ steps.meta.outputs.commit_sha }}
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.commit_target }}
          fetch-depth: 0

      - name: Resolve target commit
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          echo "commit_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  ubuntu-deb-anylinux:
    name: ubuntu deb and anylinux binaries
    needs: resolve-target
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.resolve-target.outputs.commit_sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Install packaging and cross tools
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu libc6-dev-arm64-cross dpkg-dev zip

      - name: Build deb packages and anylinux bundles
        shell: bash
        run: |
          set -euo pipefail
          CLUSTAT_DEB_MULTIARCH=1 scripts/package-deb.sh
          mkdir -p build-artifacts
          cp dist/clustat-server_*_all.deb build-artifacts/clustat-server-multiarch.deb
          cp dist/clustat-client_*_all.deb build-artifacts/clustat-client-multiarch.deb

          make_anylinux_bundle() {
            local arch="$1"
            local server_bin="$2"
            local client_bin="$3"
            local backend_bin="$4"
            local tmpdir
            tmpdir="$(mktemp -d)"
            mkdir -p "$tmpdir/bin" "$tmpdir/etc/clustat"
            cp "$server_bin" "$tmpdir/bin/clustat-server"
            cp "$client_bin" "$tmpdir/bin/clustat"
            cp "$backend_bin" "$tmpdir/bin/clustat-backend"
            cp dist/etc/clustat/server.toml.example "$tmpdir/etc/clustat/server.toml.example"
            cp dist/etc/clustat/client.toml.example "$tmpdir/etc/clustat/client.toml.example"
            (cd "$tmpdir" && zip -qr "${GITHUB_WORKSPACE}/build-artifacts/clustat-server&client-anylinux-${arch}.zip" .)
            rm -rf "$tmpdir"
          }

          make_anylinux_bundle \
            x86_64 \
            target/release/clustat-server \
            target/release/clustat \
            target/release/clustat-backend
          make_anylinux_bundle \
            aarch64 \
            target/aarch64-unknown-linux-gnu/release/clustat-server \
            target/aarch64-unknown-linux-gnu/release/clustat \
            target/aarch64-unknown-linux-gnu/release/clustat-backend

      - name: Upload server deb artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-server-multiarch.deb
          path: build-artifacts/clustat-server-multiarch.deb

      - name: Upload client deb artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-client-multiarch.deb
          path: build-artifacts/clustat-client-multiarch.deb

      - name: Upload x86_64 anylinux artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-server&client-anylinux-x86_64.zip
          path: build-artifacts/clustat-server&client-anylinux-x86_64.zip

      - name: Upload aarch64 anylinux artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-server&client-anylinux-aarch64.zip
          path: build-artifacts/clustat-server&client-anylinux-aarch64.zip

  redhat-rpm:
    name: redhat rpm packages
    needs: resolve-target
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.resolve-target.outputs.commit_sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Install packaging and cross tools
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm gcc-aarch64-linux-gnu g++-aarch64-linux-gnu libc6-dev-arm64-cross

      - name: Build rpm packages
        run: |
          set -euo pipefail
          CLUSTAT_RPM_MULTIARCH=1 scripts/package-rpm.sh
          mkdir -p build-artifacts
          cp dist/clustat-server-*.noarch.rpm build-artifacts/clustat-server-multiarch.rpm
          cp dist/clustat-client-*.noarch.rpm build-artifacts/clustat-client-multiarch.rpm

      - name: Upload server rpm artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-server-multiarch.rpm
          path: build-artifacts/clustat-server-multiarch.rpm

      - name: Upload client rpm artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-client-multiarch.rpm
          path: build-artifacts/clustat-client-multiarch.rpm

  archlinux-client:
    name: archlinux client package
    needs: resolve-target
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.resolve-target.outputs.commit_sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install packaging tools
        run: |
          sudo apt-get update
          sudo apt-get install -y zstd

      - name: Build Arch Linux client package
        run: |
          set -euo pipefail
          scripts/package-archlinux-client.sh
          mkdir -p build-artifacts
          cp dist/*.pkg.tar.zst build-artifacts/clustat-client-archlinux-multiarch.pkg.tar.zst

      - name: Upload Arch Linux artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-client-archlinux-multiarch.pkg.tar.zst
          path: build-artifacts/clustat-client-archlinux-multiarch.pkg.tar.zst

  macos-client:
    name: macos multiarch client
    needs: resolve-target
    runs-on: macos-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.resolve-target.outputs.commit_sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin,aarch64-apple-darwin

      - name: Build per-arch client binaries
        run: |
          set -euxo pipefail
          for target in x86_64-apple-darwin aarch64-apple-darwin; do
            cargo build --locked --release --target "$target" -p clustat-client-backend
            cargo build --locked --release --target "$target" -p clustat-client-cli
          done

      - name: Create universal client package
        run: |
          set -euxo pipefail
          pkgdir="package-root"
          mkdir -p "build-artifacts" "$pkgdir/bin" "$pkgdir/etc/clustat"
          lipo -create \
            target/x86_64-apple-darwin/release/clustat \
            target/aarch64-apple-darwin/release/clustat \
            -output "$pkgdir/bin/clustat"
          lipo -create \
            target/x86_64-apple-darwin/release/clustat-backend \
            target/aarch64-apple-darwin/release/clustat-backend \
            -output "$pkgdir/bin/clustat-backend"
          cp dist/etc/clustat/client.toml.example "$pkgdir/etc/clustat/client.toml.example"
          cp dist/etc/clustat/client.toml.example "$pkgdir/etc/clustat/client.toml"
          tar -C "$pkgdir" -czf "build-artifacts/clustat-client-macos-multiarch.tar.gz" .

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-client-macos-multiarch.tar.gz
          path: build-artifacts/clustat-client-macos-multiarch.tar.gz

  windows-client:
    name: windows multiarch client gnullvm
    needs: resolve-target
    runs-on: windows-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.resolve-target.outputs.commit_sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnullvm,aarch64-pc-windows-gnullvm

      - name: Install LLVM-MinGW
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $version = "20251007"
          $zip = "llvm-mingw-$version-ucrt-x86_64.zip"
          $url = "https://github.com/mstorsjo/llvm-mingw/releases/download/$version/$zip"
          Invoke-WebRequest $url -OutFile $zip
          Expand-Archive $zip -DestinationPath .
          $root = Join-Path $PWD.Path "llvm-mingw-$version-ucrt-x86_64\bin"
          echo $root | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Build client binaries
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          cargo build --locked --release --target x86_64-pc-windows-gnullvm -p clustat-client-cli
          cargo build --locked --release --target aarch64-pc-windows-gnullvm -p clustat-client-cli

      - name: Package Windows client
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          New-Item -ItemType Directory -Force build-artifacts, package-root\bin, package-root\etc\clustat | Out-Null
          Copy-Item target\x86_64-pc-windows-gnullvm\release\clustat.exe package-root\bin\clustat-client-x86_64.exe
          Copy-Item target\aarch64-pc-windows-gnullvm\release\clustat.exe package-root\bin\clustat-client-aarch64.exe
          Copy-Item dist\etc\clustat\client.toml.example package-root\etc\clustat\client.toml.example
          Copy-Item dist\etc\clustat\client.toml.example package-root\etc\clustat\client.toml
          Compress-Archive -Path package-root\* -DestinationPath "build-artifacts\clustat-client-windows-multiarch.zip" -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v7
        with:
          name: clustat-client-windows-multiarch.zip
          path: build-artifacts/clustat-client-windows-multiarch.zip
